import { useHistory } from "react-router-dom";
function Home(props) {
  const history = useHistory();

    return (
      <body>
        <p>This is the Home page.</p>
        </body>
    );
  }
  export default Home;